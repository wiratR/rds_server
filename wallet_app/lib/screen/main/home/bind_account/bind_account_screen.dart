import 'package:flutter/material.dart';

class BindAccountScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bind Account'),
      ),
      body: const Center(
        child: Text('Bind Account Functionality Here'),
      ),
    );
  }
}
